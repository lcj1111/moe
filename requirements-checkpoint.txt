# 作用：在开发依赖之外运行小型 CPU checkpoint 测试。
# 先按 README 从 PyTorch CPU 索引安装 torch，避免下载 CUDA 运行库。
-r requirements-dev.txt
torch==2.7.1
safetensors==0.5.3
# safetensors 的张量写入路径会运行时导入 NumPy，pip check 不会自动发现此项。
numpy==2.2.6
